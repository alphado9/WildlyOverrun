---
layout: layouts/base.njk
title: Wildly Overrun
---
<div class="home">
  <div class="home-hero">
    <span class="label">projects — straight talk</span>
    <h1>Every project<br>is <span>wildly</span><br>overrun.</h1>
    <p>A podcast and publication about how the world's biggest capital projects actually get built — the finance, the controls, the deals, and the people who run them. Owners, operators, private equity, and the stories behind the projects that blew up spectacularly.</p>
  </div>

  <div class="post-list">
    <span class="section-label">Latest</span>
    {% for post in collections.posts %}
      <a href="{{ post.url }}" class="post-list-item">
        <div class="item-date">{{ post.date | readableDate }}</div>
        <div class="item-body">
          {% if post.data.tags %}
            <div class="item-tags">
              {% for tag in post.data.tags %}
                {% if tag != "posts" %}
                  <span class="tag">{{ tag }}</span>
                {% endif %}
              {% endfor %}
            </div>
          {% endif %}
          <div class="item-title">{{ post.data.title }}</div>
          {% if post.data.description %}
            <div class="item-deck">{{ post.data.description }}</div>
          {% endif %}
        </div>
      </a>
    {% endfor %}
  </div>
</div>
